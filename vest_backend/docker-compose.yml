services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "8002:8000"
    volumes:
      - db-data:/app/data
    environment:
      - DB_PATH=/app/data/vest.db

volumes:
  db-data:
